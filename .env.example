# Hosted pcc-data assets root (no trailing slash).
# Leave empty to serve sibling ../pcc-data/assets via local Vite middleware.
VITE_ASSET_BASE_URL=https://ax108.github.io/pcc-data/assets

# Netlify Function email delivery (server-side only; configure these in Netlify).
SMTP_PASS=
APPLICATION_RECIPIENT_EMAIL=arindamc.ax@gmail.com
